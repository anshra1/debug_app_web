enum ErrorPriority { low, medium, high, fatal, superFatal }

enum ErrorStates { New, InProgress, Resolved, Archived }

enum ErrorSeverity { Low, Medium, High, Critical }

enum ErrorCategory { UI, Network, Database, Logic, Performance, Security }
