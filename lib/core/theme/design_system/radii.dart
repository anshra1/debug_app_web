import 'package:flutter/material.dart' show BorderRadius;
import 'package:flutter/widgets.dart' show Radius;

class AppRadii {
  static const BorderRadius r4 = BorderRadius.all(Radius.circular(4));
  static const BorderRadius r8 = BorderRadius.all(Radius.circular(8));
  static const BorderRadius r16 = BorderRadius.all(Radius.circular(16));
  static const BorderRadius r24 = BorderRadius.all(Radius.circular(24));
  static const BorderRadius r32 = BorderRadius.all(Radius.circular(32));
  static const BorderRadius r40 = BorderRadius.all(Radius.circular(40));
  static const BorderRadius r48 = BorderRadius.all(Radius.circular(48));
  static const BorderRadius r56 = BorderRadius.all(Radius.circular(56));
  static const BorderRadius r64 = BorderRadius.all(Radius.circular(64));
}
