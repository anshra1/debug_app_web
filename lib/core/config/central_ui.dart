import 'dart:ui';

import 'package:flutter/material.dart' show Colors;

class UIConfig {
  // Sidebar
  
  // dimensions
  static const double sideBarWidth = 360;
  static const double sidebarBorderRadius = 4;
  static const double sidebarListItemBorderRadius = 4;
  static const double sidebarListItemHeight = 120;

  // Sidebar Colors
  static const Color sidebarBackgroundColor = Color(0xFF1E1E1E);
  static const Color sidebarItemBackgroundColor = Colors.blueAccent;
  static const Color sidebarTextColor = Color(0xFFFFFFFF);

  // fontSize
  static const double sidebarFontSize = 16;

  // Error Container
  static const double errorContainerBorderRadius = 8;

  // fontSize

  // Top Bar
  static const double buttonBorderRadius = 4;
}
